{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/ticket-management-1.0-SNAPSHOT.jar",
    "healthcheckPath": "/api/tickets",
    "restartPolicyType": "ON_FAILURE"
  }
}
